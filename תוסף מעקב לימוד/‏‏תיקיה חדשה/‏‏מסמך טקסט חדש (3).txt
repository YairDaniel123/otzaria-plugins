2:18תכנית עבודה מסודרת! הנה איך אחלק:חלק 1 – HTML + CSS

כל תגי ה-HTML (מבנה המסכים, ניווט, דיאלוגים, כרטיסים)
כל ה-CSS (Design tokens, כל הרכיבים לפי DESIGN_GUIDE)
מסך הבית, מסך ההספק, מסך הטיימר, מסך מפת הש"ס, מסך הגדרות
כל הדיאלוגים והאוברליי
חלק 2 – JavaScript חלק א' (לוגיקה)

applyTheme + hexToRgba לפי DESIGN_GUIDE
כל ה-utils (תאריך עברי, גמטרייה, פורמט זמן)
מנוע האחסון (עטיפות אחסון)
לוגיקת תוכניות לימוד (חישוב ההספק, סימון, קידמה, היסטוריה)
מודול מפת הש"ס (עטוף בנפרד)
מנוע הטיימר (sessions, השהיה, מקביל)
חלק 3 – JavaScript חלק ב' (UI + אירועים)

כל פונקציות ה-render לכל מסך
ניווט בין מסכים
wiring של כל הכפתורים והדיאלוגים
חיפוש ספרים (library.findBooks)
ייצוא/ייבוא JSON
Otzaria.on('plugin.boot', ...) + theme.changed
תזמון התראות